- name: Core
  packages:
    - repo: bokeh/bokeh
      sponsors: [numfocus, anaconda]
      badges: travis, pypi, conda, site

    - repo: matplotlib/matplotlib
      sponsors: [numfocus]
      badges: travis, appveyor, codecov, pypi, conda, site

    - repo: plotly/plotly.py
      sponsors: [plotly]
      pypi_name: plotly
      conda_package: plotly
      conda_channel: conda-forge
      site: plot.ly
      badges: circleci, pypi, conda, site

- name: High-Level Shared API
  packages:

    - repo: pandas-dev/pandas
      sponsors: [numfocus]
      site: pandas.pydata.org
      badges: travis, appveyor, codecov, rtd, pypi, conda, site

    - repo: pydata/xarray
      sponsors: [numfocus]
      appveyor_project: shoyer/xray
      site: xarray.pydata.org
      badges: travis, appveyor, coveralls, rtd, pypi, conda, site

    - repo: dask/dask
      sponsors: [anaconda, nvidia]
      appveyor_project: daskdev/dask
      badges: travis, appveyor, coveralls, rtd, pypi, conda

    - repo: altair-viz/pdvega
      site: altair-viz.github.io/pdvega
      badges: travis, pypi, site

    - repo: pyviz/hvplot
      sponsors: [anaconda]
      site: hvplot.pyviz.org
      badges: travis, appveyor, pypi, conda, site

    - repo: PatrikHlobil/Pandas-Bokeh
      badges: pypi

    - repo: santosjorge/cufflinks
      badges: circleci, pypi

- name: High-Level
  packages:

    - repo: mwaskom/seaborn
      site: seaborn.pydata.org
      badges: travis, codecov, pypi, conda, site

    - repo: altair-viz/altair
      site: altair-viz.github.io
      conda_channel: conda-forge
      badges: travis, pypi, conda, site

    - repo: spotify/chartify
      sponsors: [spotify]
      conda_channel: conda-forge
      badges: travis, pypi, conda, site

    - repo: pyviz/holoviews
      sponsors: [anaconda]
      appveyor_project: pyviz/holoviews-ginxt
      badges: travis, appveyor, coveralls, pypi, conda, site

    - repo: plotly/plotly_express
      sponsors: [plotly]
      site: plotly.express
      badges: pypi, site

- name: Dashboarding
  packages:

    - repo: plotly/dash
      sponsors: [plotly]
      conda_channel: conda-forge
      site: dash.plot.ly
      badges: circleci, pypi, conda, site

    - repo: pyviz/panel
      sponsors: [anaconda]
      site: panel.pyviz.org
      badges: travis, appveyor, codecov, pypi, conda, site

    - repo: QuantStack/voila
      sponsors: [quantstack]
      conda_channel: conda-forge
      badges: travis, pypi, conda, rtd

- name: SciVis
  packages:
    - repo: vispy/vispy
      conda_channel: conda-forge
      site_protocol: http
      badges: travis, appveyor, coveralls, pypi, conda, site

    - repo: glumpy/glumpy
      badges: pypi, rtd

    - repo: enthought/mayavi
      sponsors: [enthought]
      site: docs.enthought.com/mayavi/mayavi
      appveyor_project: EnthoughtOSS/mayavi
      badges: travis, appveyor, codecov, pypi, conda, site

    - repo: Kitware/ParaView
      sponsors: [kitware]
      conda_channel: conda-forge
      badges: travis, conda, site

    - repo: Kitware/VTK
      sponsors: [kitware]
      conda_channel: conda-forge
      badges: travis, pypi, conda, site

    - repo: yt-project/yt
      sponsors: [numfocus]
      site: yt-project.org
      badges: travis, codecov, pypi, conda, site

- name: Color
  packages:

    - repo: pyviz/colorcet
      sponsors: [anaconda]
      site: colorcet.pyviz.org
      badges: travis, appveyor, pypi, conda, site

    - repo: matplotlib/viscm
      conda_channel: conda-forge
      badges: travis, codecov, pypi, conda

    - repo: matplotlib/cmocean
      site: matplotlib.org/cmocean
      conda_channel: conda-forge
      badges: travis, codecov, pypi, conda

- name: Domain-specific
  packages:

    - repo: networkx/networkx
      site: networkx.github.io
      appveyor_project: dschult/networkx-pqott
      badges: travis, appveyor, codecov, pypi, conda, site

    - repo: scikit-image/scikit-image
      site: scikit-image.org
      badges: travis, appveyor, codecov, pypi, conda, site

    - repo: SciTools/cartopy
      sponsors: [metoffice]
      site: scitools.org.uk/cartopy
      badges: travis, appveyor, coveralls, pypi, conda, site

    - repo: pyviz/geoviews
      sponsors: [anaconda]
      site_protocol: http
      badges: travis, appveyor, pypi, conda, site

- name: Other
  packages:

    - repo: pyviz/datashader
      sponsors: [anaconda]
      site_protocol: http
      badges: travis, appveyor, pypi, conda, site

    - repo: vaexio/vaex
      sponsors: [vaex.io]
      site: vaex.io
      conda_channel: conda-forge
      badges: travis, appveyor, pypi, conda, site
